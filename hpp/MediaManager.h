#pragma once
#ifndef MEDIAMANAGER_H
#define MEDIAMANAGER_H

#include "Base.h"

class MediaManager {
private:
    string filePath;

public:
    MediaManager();
    ~MediaManager();
};

#endif // MEDIAMANAGER_H